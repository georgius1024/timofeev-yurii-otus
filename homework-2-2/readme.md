# About
Вторая домашняя работа в потоке javascrit-2018-10 OTUS

# Description
Написать функцию `promiseReduce`, которая получает на вход 
- массив асинхронных функций `asyncFunctions`, возвращающих `Promise`, 
- `reduce` функцию и 
- стартовое значение `initialValue`.

# How to
Подразумевается использование в среде *node*
Для проверки запускаем node .
